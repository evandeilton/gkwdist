.Rproj.user
.Rhistory
.RData
.Ruserdata
gkwdist.Rproj
src/*.o
src/*.so
src/*.dll
inst/doc
TODO.md

# Dropbox sync conflict copies
*Cópia em conflito*
*conflicted copy*

# LaTeX build artefacts from local paper compiles
inst/paper/*.log
inst/paper/*.aux
inst/paper/*.out

# Claude Code session state. .claude/worktrees/ holds linked git worktrees;
# committing one records a gitlink that no clone can resolve.
.claude/

# Agent scaffolding. The files stay on disk -- CLAUDE.md points at them -- but
# they are not part of the package's published source.
.agents/
